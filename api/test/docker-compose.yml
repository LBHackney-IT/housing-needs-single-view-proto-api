version: '3'
services:
  db:
    image: 'mcr.microsoft.com/mssql/server'
    ports:
      - '1433:1433'
    volumes:
      - ./sql/UHT/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    environment:
      SA_PASSWORD: 'UHT_password'
      ACCEPT_EULA: 'Y'
